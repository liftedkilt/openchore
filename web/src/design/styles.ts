// Global styles for the design system: self-hosted fonts, generated tokens,
// theme dials and component classes. Imported once, from the design entry.
//
// None of these redefine the legacy variables in styles/globals.css
// (--bg-primary, --radius-md, …), so existing pages keep their look.
import '@fontsource-variable/onest';
import '@fontsource-variable/fraunces/full.css'; // wght + opsz + SOFT + WONK
import '@fontsource-variable/fraunces/full-italic.css';
import '@fontsource-variable/figtree';
import '@fontsource-variable/bricolage-grotesque/opsz.css'; // wght + opsz
import '@fontsource-variable/instrument-sans';
import './tokens.css';
import './themes.css';
import './components.css';
