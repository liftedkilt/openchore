One person's progress today, for family views: avatar, name, a bar in their colour and a count.

- **Props:** `name`, `color`, `done` and `total`.
- Family views live on shared screens, so they normally render in House.
- List everyone in a fixed order (the family's order, not by score). This shows progress, never a ranking.
- The bar is identity-coloured and can be low-contrast in Sunroom's pastels. The written count carries the value.
