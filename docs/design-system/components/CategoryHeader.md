The label above a group of chores: the category's shape, its name and a count.

- **Props:** `cat` (`essential`, `daily` or `bonus`), an optional `count` ("2 of 3", "+15") and an optional `label` override. Don't override the label; the names are shared across skins.
- The shape (circle, square, star) is the same in every skin and carries the meaning. The colour is a bonus: Tint keeps all three grey.
