Lily's Today screen rendered three times, once per skin, from identical components and identical data. Only `data-theme` on the device changes.

Compare them on:
- the same order, tabs, rows and words;
- a different hero (sun arc, shape row, ring);
- different type, surfaces and outlines;
- Lily's mint running through all three, and leading the whole screen in Tint.
